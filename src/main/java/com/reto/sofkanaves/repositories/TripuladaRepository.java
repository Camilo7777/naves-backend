package com.reto.sofkanaves.repositories;

import com.reto.sofkanaves.models.Tripulada;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TripuladaRepository extends JpaRepository<Tripulada,Long> {
}
